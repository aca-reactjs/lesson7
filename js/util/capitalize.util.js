const capitalize = (str) => {
  return str[0].toUpperCase() + str.slice(1);
};

const FIRST_LETTER = "A";

export { FIRST_LETTER as A };

export default capitalize;

if (true) {
  console.log("Hi");
}
